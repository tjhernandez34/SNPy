<%= link_to "upload genome", new_genome_path %>
<p>Welcome <%= current_user.first_name %></p>

<% if @categories %>
  <% @categories.keys.each do |category| %>
    <p><%= link_to "#{category.name}", category_path(category) %></p>
  <% end %>
<% end %>

<div id="zoomable_sunburst">
</div>



